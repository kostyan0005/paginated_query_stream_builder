import 'package:flutter/material.dart';

// todo: define
///
abstract class QueryStreamView extends StatelessWidget {
  const QueryStreamView();

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
